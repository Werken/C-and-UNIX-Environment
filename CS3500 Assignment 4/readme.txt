Save all files in the same location. To compile all the files, type:

>>make

To run the program, type:

>>./compress "input filename" "output filename"

Replacing "input filename" with girl.pgm, mandrill.pgm, or casablanca.pgm

"output filename" can be any word as long as it ends with .pgm